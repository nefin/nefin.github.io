---
title: "People"
subtitle: "Faculty, researchers, students and alumni of NEFIN"

# These sections have no per-item pages: every item is rendered inside its
# list. Without this, Hugo emits ~60 title-only pages into the sitemap.
cascade:
  - target:
      kind: page
    build:
      render: never
      list: local
---
